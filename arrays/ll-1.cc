#include <vector>

using namespace std;

class Solution {
public:
  vector<int> twoSum(vector<int> &nums, int target) {
    int var = 0;
    int count = 0;

    for (int i = 0; i < nums.size(); i++) {
      var += nums[i];
      
    }
  }
};