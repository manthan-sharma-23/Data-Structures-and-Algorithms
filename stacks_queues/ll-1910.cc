#include <stack>
#include <string>

using namespace std;

class Solution {
public:
  string removeOccurrences(string s, string part) {
    int n = s.length();
    stack<char> st;

    int i = 0;

    string temp = "";

    

    while (i < n) {
    }
  }
};