#include <string>

using namespace std;

class Solution {
public:
  string minWindow(string s, string t) {
    int n1 = s.length(), n2 = t.length();

    string res = "";

    int i = 0;
    for (int j = 0; j < n1; j++) {
    }
  }
};